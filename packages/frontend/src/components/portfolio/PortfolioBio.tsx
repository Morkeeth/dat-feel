import * as React from 'react'
import { FC } from 'react'
import { Text } from '@geist-ui/react'

const PortfolioExperience: FC = ({ user }) => <Text mt={0}>Turning coffee into code 👨‍🌾</Text>

export default PortfolioExperience
